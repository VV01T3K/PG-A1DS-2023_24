#include <cstdio>

int main() {
    int k;  // number of graphs

    scanf("%d", &k);

    for (int i = 0; i < k; i++) {
        // int n;  // number of vertices
        for (int j = 0; j < 10; j++) {
            printf("?\n");
        }
    }

    return 0;
}